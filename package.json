{
  "name": "redline-employee-portal",
  "version": "1.0.0",
  "description": "Simple employee portal for Redline: self-signup with admin approval, plus office-day scheduling. Express API in front of Supabase (Auth + Postgres).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1"
  }
}
